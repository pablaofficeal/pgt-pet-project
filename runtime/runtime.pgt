package runtime

from "components/logging" import to_file, info
from "models/init" import migrate

function(setup ){
    log_file("server.log")
    info("starting backend")
    sql::open("app.sqlite")
    migrate()
    return 1
}
